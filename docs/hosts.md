# Existing host inventory and baseline transition

Read-only discovery: **2026-09-09 UTC**. All three machines were already installed. No disk script, mount, partition/format/repair, pool import/export, activation, installation or reboot was executed. SSH used existing known-host entries with strict checking and no automatic host-key updates. No private keys, password hashes, VPN state or Wi-Fi secrets were collected.

## Current status

**Authoritative status, reconciled 2026-09-11 UTC from the dated evidence below; no new boot or service acceptance in this review.** Other runbooks link here rather than restating acceptance. Historical inventory/checkpoints describe their own date and may be superseded.

- **ThinkPad configuration/boot:** commissioned, `ready = true`, local-only, the sole `nixosConfigurations` output. [Two-boot acceptance](#first-boot-acceptance-2026-09-10-utc) and the later [maintenance boot](#maintenance-boot-observation-2026-09-10-utc) are recorded. Current dependency/Pi/Tailscale changes and the output subscriber-first guard plus Lua IPC correction remain unactivated. Maintenance checks did not accept real hotplug/lid/HDR/VRR transitions, fixed-bar visuals, Pi/Herdr/Zed, Bitwarden, KDE Connect, printing/scanning or other peripheral workflows; retain the accepted generation for separately authorized follow-up. NVIDIA/eGPU topology remains unverified.
- **ThinkPad credentials/network:** the 2026-09-10 private audit and operator-confirmed independent recovery support identity review. The subsequent two boots verified early password/runtime Wi-Fi delivery, MN-Home system-owned autoconnect and removal of the old helper/file-secret agent. Campus profile delivery is verified, **SenecaNET authentication and certificate-rejection behavior on campus are not**. Optional fingerprint enrollment remains open. These results do not verify the later Tailscale credential or new candidate boot.
- **Shared password candidate (2026-09-11):** the user selected the existing ThinkPad password for all three `marcos` accounts. An explicitly authorized local memory-only transfer verified MACs/hash equality and produced [password-only shared YAML](../secrets/README.md#shared-marcos-password), with only the existing operator/ThinkPad/Racknerd recipients. Original Wi-Fi/host ciphertext is unchanged. No activation or installed-password change occurred; Bastion still has no verified identity/recipient and its candidate account remains locked. Earlier boot evidence does not establish delivery from this new source.
- **Racknerd:** `ready = false`, absent from standard NixOS/deploy outputs. [Authorized temporary bootstrap](#authorized-access-bootstrap-and-sops-preparation) verified marcos key login and operator-tested password sudo while retaining root SSH/old generation; the initial root-only inventory is superseded, not the target root-login prohibition. Dedicated identity and password ciphertext are declared, but early decryption/recovery and closure-trust provisioning remain unresolved. The saved encrypted identity recovery copy is not a tested restore. [Commissioning status and disabled-Tailscale acceptance](#commissioning-review-status-2026-09-10) remain binding.
- **Bastion and remote deployment:** Bastion remains unready and still needs staged non-root access, credentials, NAS import/restore and other transition reviews. The operator reports both remote deployments did not complete; [deferred work](#deferred-remote-host-deployment--2026-09-11) may involve a separately designed/authorized reinstall. No current deploy nodes are exported.
- **Tailscale:** a prior policy-only apply, no-drift verification, independent access and protected backups were [operator-reported complete](validation.md#operator-reported-policy-apply-and-non-saving-verification--2026-09-10), not agent-run API/traffic tests. The current checked policy differs from that applied evidence and has not been planned, applied or live-verified. Only ThinkPad's unactivated auth-key candidate is enabled, with operator-confirmed key preparation and recoverable retained host-state backup. Runtime key decryption/enrollment, durable identity/tag cardinality and allowed/denied traffic remain unverified. Racknerd/Bastion candidates disable the service/enrollment and preserve backing state; the historical `NeedsLogin` daemons are not a required post-activation state. See [rollout procedure](tailscale.md).

## Scope and chosen policy

- NixOS with console/emergency recovery, key-only SSH, `marcos` administration with **authenticated sudo/password fallback** (ThinkPad additionally permits fingerprint), firewall, time synchronization and explicit persistence. Normal account shells default to Bash except the approved ThinkPad Zsh configuration; existing home contents remain intact. No passwordless sudo or blanket Nix trust.
- Limine on all three; tmpfs `/`, keeping existing durable filesystems. Thinkpad retains its encrypted LUKS/LVM stack and swap. No old Btrfs root-deletion hook is copied; existing root subvolumes remain on disk for recovery.
- Server nftables, conservative kernel hardening and bounded persistent journals. Racknerd additionally gets a journal-backed fail2ban SSH jail with persistent ban database. TCP 22 only; no HTTP(S), shares, reverse proxy or other public application yet.
- ThinkPad uses NetworkManager, UPower and power-profiles-daemon (not TLP/auto-cpufreq), plus Intel thermald, Wi-Fi power saving and Bolt with its existing authorization state. No blanket Thunderbolt authorization, eGPU driver guess, Steam, 32-bit gaming stack or performance tuning. No charge thresholds are changed. ThinkPad now explicitly opts into a **Hyprland/Noctalia/Greeter desktop with approved app preferences, fingerprint and SOPS Wi-Fi**, described in [the desktop procedure](desktop.md); both servers stay headless. These are **not tested gaming configurations**.
- Explicit latest stock **7.x kernels from each host's own track**: 7.2.4 on all three hosts at current pins. OpenZFS 2.4.4 supports 7.2; compatibility failures stay enabled. Derivations, not boots, are tested; no pool features or driver force-probes change.
- [Tailscale rollout and policy status](#current-status) are separate from OS readiness. Previously persisted VPN/service state is left on backing storage, not deleted; do not treat service removal as key revocation.
- Deploy-rs intent: racknerd and bastion, using `marcos`, root activation and interactive sudo. Thinkpad stays local-only. Closure transport/trust remains unresolved rather than granting root-equivalent Nix access automatically.
- [Current status](#current-status) distinguishes configuration gates, deployed generations and still-open runtime acceptance.

## Deferred remote-host deployment — 2026-09-11

The operator reports that deployment of `racknerd` and `bastion` did not complete. This is an intentionally deferred runtime/host-state task, not a failure of the repository's local evaluation or canonical check suite. No retry, reinstall, activation or reboot is performed by this branch. A future attempt will use another branch/time, and reinstalling these two hosts is likely; any reinstall needs its own reviewed, explicitly authorized fresh-install design. The current facts and readiness gates are not evidence of successful remote deployment or boot acceptance.

## Storage representation

`existing-storage` uses the locked disko `nodev` type to derive runtime filesystems for existing devices. Despite its name, that type accepts a device and filesystem options and performs no filesystem creation. Disk/GPT/LVM-create/ZFS-create collections are closed and all `system.build` disko script/image/installation-test aliases are rejected, **even after readiness**. `devenv shell disk-plan HOST` refuses existing installations. This is deliberately **not** a full install/reformat layout.

Thinkpad's existing LUKS unlock and LVM activation are ordinary NixOS initrd configuration, not disko format instructions. Bastion's existing ZFS legacy mounts are ordinary NixOS `fileSystems`, outside disko. Runtime activation can still change boot files and a later boot mounts these declarations; configuration evaluation is not a migration rehearsal.

The old `os-disk` capability and its synthetic UEFI/BIOS/ESP rejection tests remain for a separately authorized **fresh installation**. None of these three hosts compose it. Never replace `existing-storage` with it to get an installation script.

Inspect the exact configuration, without touching devices:

```sh
devenv tasks run repo:inventory
nix eval --json .#fleet.bastion.filesystems | jq .
nix eval --json .#fleet.thinkpad.persistence | jq .
nix eval --json .#deploymentPlan | jq .
```

## Observed machines

Initial inventory is **2026-09-09 UTC** unless a later observation is explicitly identified. Review flags, running versions and access descriptions here are historical; use [current status](#current-status) and subsequent dated evidence for today's acceptance.

### thinkpad — this machine

- Lenovo ThinkPad T14 Gen 3, model 21AH00BNUS; Intel i7-1270P, ~38 GiB usable RAM, Intel i915 graphics (`8086:46a6`). Thunderbolt controllers present with `user` authorization security; eGPU disconnected at discovery. Previous configuration mentions an RTX 3070, but that is **not live verification** and no driver is carried over.
- Desktop follow-up observed only `eDP-1`, Chimei Innolux `0x143F`, **1920×1200 @ 60.003 Hz**, scale 1; Intel PCI `0000:00:02.0`. Bolt lists a disconnected Razer Core X. No Samsung EDID/connector/mode or NVIDIA PCI ID was observed. Display/layout/shell defaults were new; the user subsequently approved reusing the requested app preferences. `fleet.desktop.reviewed` was false at discovery; its later review and boot evidence are recorded below.
- Fingerprint follow-up: Synaptics USB **06cb:00f9**, present in pinned libfprint's device table. No enrollment/template contents were read, copied or changed. Password/fingerprint/keyring and docked behavior remain acceptance items.
- Existing printer configuration names Epson ET-3850 at `ipps://192.168.4.20:631/ipp/print`; no live printer/scanner/TLS test. MN-Home's encrypted PSK/profile identity is reused. SenecaNET's privately filled ciphertext is now selected following the audit below; campus authentication and certificate rejection remain untested.
- UEFI, Limine, Secure Boot variable off. Existing NVRAM-write policy is preserved. The active/first EFI entry and ESP/capacity/health metadata are now reviewed alongside operator-confirmed console/rescue recovery; `bootReviewed = true` records this preflight, not a candidate boot test.
- Micron 512 GB NVMe, by-id `nvme-eui.00a075013a594e93`. ESP UUID `E0BE-02B0`, 4 GiB, ~119 MiB used at discovery.
- LUKS UUID `072d4bf0-1930-4203-a5ec-1430e2e67c71`, mapper `crypted`, VG `vg`. Btrfs LV `/dev/mapper/vg-system`, filesystem UUID `ed7306e0-58eb-4d13-891d-0cf3cd0fa017`; existing subvolumes `root`, `nix`, `persist`, `home`. Retain the latter three; target root is tmpfs. Preserve `nodiscard` mount policy and the existing discard-capable LUKS mapping; no new scheduled trim job.
- Existing 48 GiB swap LV, UUID `df87ed65-251d-4e5a-8451-f7df56f2819c`; resume device `/dev/vg/swap` retained. Hibernation with this new initrd/root is **not tested**.
- `marcos` UID 1000. NetworkManager Wi-Fi/Ethernet; preserve existing connection profiles, not a hardcoded lease address.
- `stateVersion = "26.05"` from prior installation source. Live version `26.11.20260905.c043004`; the target uses this repository's locked unstable input instead.
- The initial unprivileged hardware generator could not inspect Btrfs. Both later privileged scans are reviewed and adapted. Their subvolume diagnostics are explained by the scanner's bind-mount fallback, confirmed in installed and target-pinned source; `hardwareReviewed = true`. This is not a built/booted initrd.

#### Partial commissioning preflight (2026-09-10 UTC)

**First-pass record:** some pending items below were subsequently resolved by the read-only follow-up in the next subsection.

This was an authorized **read-only** local inspection and operator-run, memory-only SOPS verification, not activation or state migration. Only sanitized metadata/booleans were returned. The ciphertext files were then checked unchanged against the audited ciphertext digests; no private value/hash/key was printed or copied into Git/store inputs.

- **Existing account:** UID 1000, `/home/marcos`, Zsh and the selected public SSH key match. The operator confirmed working LUKS unlock, password console login and Limine/rescue recovery. Private comparison matched the encrypted password hash to installed shadow and the early root-only runtime file. Target privileges remain password/fingerprint sudo, locked root, key-only non-root SSH and root-only Nix trust, plus the already-approved scanner/lp groups. This supports **`fleet.workstation.usersReviewed = true`**, not new PAM/desktop acceptance.
- **Secrets:** dedicated age key is root-owned `0600`, parent `0700`, and its public recipient matches. Both SOPS MAC/decryption checks passed without fallback identities. MN-Home PSK matches its current root-only runtime file. Both campus values are filled, single-line and marker-free, with username-only identity; The desktop Wi-Fi concern now selects the separate campus file (currently `modules/desktop/wifi.nix`, relocated without changing the audited selection). Actual campus account/server validation and new-boot early delivery remain untested; **`identityReviewed` stays false** pending the remaining custody/recovery/boot review.
- **Hardware/boot:** privileged stdout-only scan returned the NVMe/Thunderbolt/USB-storage/keyboard/SCSI list plus `dm-snapshot` and `kvm-intel`; all are now represented. The scan exited zero but emitted diagnostics suppressed by the audit: inspect these before acknowledging hardware review. Btrfs device error counters are all zero, no device is missing and allocation has substantial free space; this is not a scrub, SMART or filesystem-integrity test. Secure Boot is off. Limine files and generations 39–45 exist, with current/booted/system profile agreeing on the existing generation. No firmware entry, boot file or generation was changed.
- **Persistence:** live/backing identity matches for Bolt, fingerprints, AccountsService, greeter, Bluetooth, NixOS allocation, timers, NetworkManager, backlight, rfkill, machine ID and random seed. SSH private/public backing files exist with `0600`/`0644`; their configured new `/etc/ssh` paths are currently absent, not evidence of missing backing keys. Only the public fingerprint was inspected. `/persist` is root-owned `0700`; its `/etc`, `/var` and `/var/lib` backing parents are `0755`.
- **Concrete migration gaps:** populated `/var/lib/cups`, `/var/lib/systemd/timesync` and `/var/lib/power-profiles-daemon` have **no matching `/persist` backing**. Preserve/reconcile these under an explicitly approved migration before tmpfs-root boot, or explicitly review any intended reset; do not silently discard them. AccountsService is currently `0775`, while the candidate requests `0700`. Pinned impermanence applies a declared mode only when creating a missing backing directory and otherwise mirrors the existing backing mode onto the ephemeral side, so correcting this requires an explicitly authorized one-time backing `chmod 0700` during migration; the candidate cannot fix it at activation. Existing greeter configuration/state also needs reconciliation. No copies, permission changes or service stops were performed. The current desktop is **niri on Btrfs `/root`**, not the proposed Hyprland/tmpfs-root system.
- **Home/network:** metadata audit found 29 existing HM-managed links and 15 absent targets, with no apparent collisions/unknowns; it is not an activation or private-profile migration test. MN-Home currently uses the same runtime profile path/UUID, but agent-owned `psk-flags = 1`; the candidate changes that to system-owned `0`. No duplicate MN-Home profile was visible in NM's profile list; preserve unrelated profiles. The old wait-online unit failed and `thinkpad-wifi-connect` timed out despite the current working connection. The latter unit is absent from the candidate. Diagnose/review startup behavior rather than declaring those failures harmless; no network connection or profile was changed. The declared printer endpoint is still unverified.
- **Remaining gates:** the operator declined making a backup; no independent backup/restore or protected SOPS recovery-copy evidence was supplied. Do not represent that decision as verification. Boot, migration, network, hardware diagnostics and desktop acceptance remain open. `ready = false`, local-only deployment intent, empty standard outputs and all other hosts are unchanged. No host build, boot update, migration, enrollment or activation was performed.

#### Read-only follow-up (2026-09-10 UTC)

Starting point: operator-committed `9c7b91e`. The follow-up report is dated **01:02 UTC**; it queried hardware/firmware/health and summarized logs without exposing credentials. No decryption, self-test, repair, firmware write, profile installation, connection change or service operation was performed in this follow-up.

- **Hardware review acknowledged:** all 15 scanner diagnostics are `Not a Btrfs subvolume: Invalid argument`, with exit zero. Installed and target-pinned scanner source deliberately probes same-device mount points, then treats failed subvolume queries as ordinary bind mounts; failure on an actual subvolume would abort. No generated filesystem stanza was imported. The later scan lacks the earlier USB-storage/SCSI entries; retain that previously observed support rather than remove it based on one scan.
- **Boot preflight acknowledged:** current/first EFI entry **0001** is active and references ESP partition GUID `b09bdcaa-c2d7-4ec2-8fd9-c50d9de1c6f9` and `\efi\limine\BOOTX64.EFI`. The mounted ESP has about **3.9 GiB free**. NVMe SMART reports health passed; the selected current-boot Btrfs/FAT/NVMe log summary has no matched errors/warnings. Combine these with the earlier zero Btrfs device counters and operator-confirmed LUKS/password console/Limine-rescue access. This is not offline filesystem verification, a restore test or proof of a new kernel/initrd boot.
- **SOPS identity review acknowledged:** the operator now confirms privately tested recovery independent of ThinkPad's disk. Combined with dedicated-key permissions/recipient/MAC/decryption checks, identical installed shadow/early-runtime binding and reviewed early `/persist`/activation ordering, this supports `identityReviewed = true`. No off-disk key contents or recovery location were requested. The new tmpfs-root boot and new runtime network delivery still require acceptance; this does not bypass those other gates.
- **Network review remains open:** unprivileged, in-memory classification could access the same 33 startup records as the privileged report, without printing raw messages. Six `nmcli` warnings say it cannot prompt for credentials; each attempt times out, followed by the helper's two-minute timeout and a supplicant association timeout. The file-secret agent was already running before the first attempt, so absence of the service is not an established cause. The remaining `<error>`-classified record is NetworkManager's per-device IPv4-forwarding sync on the transient `p2p-dev-wlp0s20f3` virtual device (`error setting IPv4 forwarding to '0': Resource temporarily unavailable`). It is a widespread, warning-level message that appears while transient devices appear or vanish and is unrelated to credentials or profile delivery; the candidate runs the same NetworkManager 1.58.0, so it may recur harmlessly. The old helper's source retries six times with a 15-second `nmcli` wait and 5-second sleep. The candidate removes this loop and agent-owned home/campus secrets, but has not demonstrated the new connection path live. No assertion of a wrong PSK or a proven fix is made.
- **State review remains open:** CUPS `printers.conf` (547 bytes) and `classes.conf` (109 bytes) are mutable, not store-backed. Main CUPS configuration and `greeter.toml` are store-backed links. The old source deliberately describes CUPS state as ephemeral, but extra live printer settings were not inspected or copied. The operator explicitly selected **regeneration of all three directories as the first-boot plan**, without carrying over extra printer settings. Recreate the declared Epson queue when its endpoint is available and let clock/power state be initialized normally. Preserve `/home`, existing persistent identities and the old Btrfs root; do not copy or delete these live directories now. This plan choice explicitly did not authorize a reset, copy or boot. The earlier backup decline is not a restore test; remaining state/permission and backup/restore review still blocks `migrationReviewed`. AccountsService's existing `0775` backing also needs that separately authorized backing-mode correction.
- **Remaining:** desktop, migration and network review, plus `ready = false`. No host build, activation or boot update has occurred; no other host's review flags changed.

#### Commissioning decisions (2026-09-10 UTC)

The operator authorized the completion sequence with explicit choices: **no new backup** (mitigations: preserved old Btrfs root and Limine generations 39–45, operator-confirmed LUKS/console/rescue access, tested independent SOPS recovery, untouched `/home`), **first-boot regeneration** of CUPS/clock/power-profile state, a **one-time backing `chmod 0700`** for `/persist/var/lib/AccountsService` (impermanence cannot correct an existing backing mode), recording `networkReviewed`/`desktopReviewed` as **pre-activation reviews with mandatory first-boot acceptance**, and agent commits of documentation/review batches only.

- **Network review recorded:** the candidate provides system-owned `MN-Home` (same UUID, `psk-flags = 0`, autoconnect) and `SenecaNET` (PEAP/MSCHAPv2, system CA bundle, `senecapolytechnic.ca` suffix match, empty anonymous identity, autoconnect false) from root-only `0400` SOPS secrets through the ordered `fleet-wifi-environment` → `NetworkManager-ensure-profiles` units; the legacy helper and file-secret agent are absent. Live home/campus connection is a first-boot acceptance test; the pre-boot helper timeouts belonged to the removed agent-owned path.
- **Desktop review recorded:** evaluated greeter session (`noctalia-greeter`, no autologin), password-first PAM with bounded fingerprint fallback and keyring hooks, Hyprland 0.56.2/UWSM, portals, PipeWire, lock/idle policy, scoped KDE Connect exposure and Bluetooth service with `powerOnBoot = false`. Live session behavior, fingerprint enrollment and peripherals are first-boot acceptance tests. Upstream emits an obsolete-`enableKwallet` deprecation warning (not from this repository); revisit on dependency updates.
- **Status:** network/desktop reviews committed through `7ad927d`; the operator then executed the AccountsService backing correction, verified as `700 root:root` on the bound `/persist/var/lib/AccountsService` subvolume path; `migrationReviewed` and `ready` are now recorded (`2104d5e`). `just ready thinkpad` passes with no missing items or failed assertions, and `just build thinkpad` produced the local toplevel closure `xvqmmah…-nixos-system-thinkpad-26.11.20260908.422d1ae` (kernel, initrd, `switch-to-configuration`, Limine boot files).
- **Boot switch (operator, from console):** on the committed tree run `sudo nixos-rebuild boot --flake .#thinkpad --no-update-lock-file`, then reboot. Limine retains the existing generations as the boot fallback.
- **First-boot acceptance (record results before normal use):** LUKS unlock; greeter password login into the Hyprland (UWSM) session; optional, separately authorized `fprintd-enroll marcos`; MN-Home autoconnect with system-owned secrets; `SenecaNET` manual connect with certificate validation while at Seneca; audio/portals/lock-idle/lock-before-suspend; KDE Connect pairing; the Epson queue once its endpoint is reachable; an ephemeral marker disappears and persisted markers survive a second boot; the previous generation still boots from Limine.

#### First-boot acceptance (2026-09-10 UTC)

The operator ran the boot switch and rebooted; generation **46** boots toplevel `xvqmmah…-nixos-system-thinkpad-26.11.20260908.422d1ae` with `/run/booted-system` = `/run/current-system`, and the live session is **Hyprland (UWSM)** after greeter password login.

- **Verified:** `/` is tmpfs; `/nix`, `/persist`, `/home` are the intended `vg-system` subvolumes (zstd, noatime); `/boot` is the vfat ESP with `fmask=0077`; machine ID present. MN-Home autoconnected with system-owned secrets (`psk-flags = 0`); SenecaNET exists with the reviewed strict policy (autoconnect off, PEAP, system CA bundle, suffix match, `password-flags = 0`); all four SOPS runtime secrets are delivered, including the early password. **Zero failed units**; the legacy wait-online failure is gone (now active) and the old helper/agent are absent. PipeWire/wireplumber, all three portals, `noctalia.service` and the Bitwarden autostart run; CUPS is socket-activated with the declared `Epson_ET-3850` default queue (endpoint not reachable from this network). Persisted modes match declarations for fprint (`0700`), greeter (`0750`, greeter-owned) and bluetooth (`0700`); clock and power-profile state regenerated as planned.
- **Found and fixed on first boot:** AccountsService regressed to `0775` because the upstream unit hardcodes `StateDirectoryMode=0775`, which systemd re-enforces on every start; the desktop module now forces `0700` (verifies on next boot). Bluetooth came up `AutoEnable=false` but a session client powered the adapter afterward; the boot default remains off. `thermald` exits `0` by design on this platform (`dytc_lapmode`); the boot journal contains only the five expected cosmetic P2P forwarding warnings and zero unclassified errors.
- **Second boot (generation 47, toplevel with the mode fix) — verified:** AccountsService is now `700 root:root` on the bound persisted path, confirming the `StateDirectoryMode` override through a real boot. The tmpfs marker was gone after reboot, MN-Home autoconnected again, Bluetooth stayed unpowered (`AutoEnable=false` honored; the earlier powering was a session-client action), zero failed units, and clock/power-profile state regenerated fresh on both boots. Persistence is independently evidenced by the bind-backed state surviving both boots (AccountsService/fprint/greeter/bluetooth backing, sshd starting from its persisted host keys); `/persist` itself is root-only `0700`, so a literal marker listing needs sudo.
- **Remaining (normal operation, not commissioning blockers):** `SenecaNET` connection test while at Seneca; optional `fprintd-enroll marcos`; optional root-only confirmations (persisted marker listing, Limine entries/NVRAM order, `credentials.env` `0600`, firewall ruleset summary).

#### Post-commissioning update candidate (2026-09-10 UTC)

A later maintenance candidate updates both Nixpkgs tracks, Home Manager, the explicit sops-nix source pin, pi-codex-api and YouTube Enhancer; its unstable package delta also advances Claude Code 2.1.263 → 2.1.266, Herdr 0.8.2 → 0.9.0 and Zed 1.17.2 → 1.19.2. Exact revisions and compatibility findings are in the [research ledger](research.md#full-dependency-and-thinkpad-display-policy-update--2026-09-10). It also requests a fixed Noctalia bar, touchpad typing suppression (already enabled), Bluetooth powered at boot and Pi default `openai-codex/gpt-6-astra`. Legacy Home Manager collision handling and Zed's prior explicit-submit search behavior remain selected; the Herdr Pi hook is unchanged.

Live read-only output/EDID inspection now confirms the internal panel and a connected Samsung Odyssey at preferred 5120×1440 with PQ/BT.2020 HDR metadata. Non-mutating policy dry-runs exercised both observed lid states: open placed the external above and centered eDP-1 below, while closed kept the external's 10-bit `hdredid` rule and emitted `eDP-1,disable`. HDR remains gated per live EDID.

The initial update candidate passed `just check` (including the native Herdr 0.9.0 config check), `just ready thinkpad` with no missing items or failed assertions, and `just build thinkpad`, producing `/nix/store/sa3s8s9ilhljccs55dgk4cj4i5wf1jds-nixos-system-thinkpad-26.11.20260910.d5dfd8e`. The subsequent display/printer review fixes passed the same gates and built `/nix/store/xbc80yn0vbmy296bifmqxijxxlpsk2lx-nixos-system-thinkpad-26.11.20260910.d5dfd8e`.

#### Maintenance boot observation (2026-09-10 UTC)

The operator deployed and rebooted into the review-fix generation. Read-only inspection confirms `/run/current-system` and `/run/booted-system` both resolve to `xbc80…`, with zero failed system/user units and zero reported Hyprland configuration errors. Noctalia and Bluetooth are active; the adapter is powered with `AutoEnable=true`; touchpad typing suppression is live; and the generated Noctalia config has a fixed bar. Deployed Pi/Zed settings select `openai-codex/gpt-6-astra` and explicit-submit search, and Herdr reports 0.9.0 with the reviewed Pi-hook digest. These are configuration/service checks, not visual or application workflow acceptance.

The cold docked display state exposed unapplied rules: with the lid closed and `DP-3` active at its preferred 5120×1440 mode, eDP-1 remained active, the outputs stayed side by side and both used 8-bit `XRGB8888`. The watcher was alive, while its non-mutating dry-run emitted the intended 10-bit `hdredid` external rule followed by `eDP-1,disable`. This was initially attributed to startup publication timing; the subscriber-first settling guard tests that timing separately. **The 2026-09-11 pinned-source review found an additional protocol error:** legacy `hyprctl keyword monitor` is rejected by the selected Lua parser, with exit zero and previously discarded output. The branch now uses acknowledged Lua monitor updates; a delay alone cannot repair incompatible IPC. Neither correction is deployed or modeset-tested.

Manual-only printer behavior also held: `ensure-printers.service` was inactive and no automatic endpoint query occurred, but CUPS currently reports no destination/default. Printing therefore remains intentionally unprovisioned until the separately authorized [manual procedure](desktop.md#home-printer-provisioning) runs while the verified printer is reachable.

The system boot journal's only `XDG_RUNTIME` record is emitted by `noctalia-greeter` under UID 998 and reports its wrapper-provided `/tmp/noctalia-runtime-998`; the normal user journal has no match. This attributes the nonstandard runtime environment to the greeter session, but the on-screen notification emitter itself is still not logged, so no warning is suppressed.

After adding the guard, locked-shell `just fmt`, full `just check`, `just ready thinkpad` and `just build thinkpad` all pass. The new unactivated toplevel is `/nix/store/mp5piyp4cp7cr148g169zkkqna2qsi2f-nixos-system-thinkpad-26.11.20260910.d5dfd8e` (derivation `/nix/store/ilp6xd1kgqprv3142sbzkjxs4m68f94w-nixos-system-thinkpad-26.11.20260910.d5dfd8e.drv`).

Maintenance acceptance remains open for the startup guard and all real hotplug/lid/HDR transitions, fixed-bar visuals, Pi/Herdr/Zed workflows, Bitwarden, KDE Connect, printing/scanning and the other application/peripheral checks. The earlier accepted generation remains the required fallback for any follow-up activation.

### racknerd — `72.11.150.242`

- KVM, five vCPUs, ~5.8 GiB RAM; BIOS boot. Stdout-only hardware scan adapted with the target Nixpkgs QEMU guest profile.
- Single 100 GiB VirtIO `/dev/vda`, no serial/whole-disk by-id. BIOS metadata partition 1; separate 2 GiB FAT `/boot`, UUID `2FE0-AD99`. Limine BIOS target `/dev/vda`, `partitionIndex = 1`; **verify again before any bootloader activation**.
- Btrfs UUID `0ccb8eeb-7ca2-4ba9-871b-e621a921ee49`, subvolumes `root`, `nix`, `persist`. No swap observed/added. Target root tmpfs; `/nix` and `/persist` kept.
- Networkd DHCPv4, MAC `00:16:3c:ec:fa:6f`; lease was `72.11.150.242/24`, gateway `.1`, DNS obtained via DHCP, RA off and IPv6 link-local retained. Configuration preserves DHCP rather than pinning the lease. Provider console/recovery remains unverified.
- **At initial discovery**, access was root-only key SSH. The [subsequent authorized bootstrap](#authorized-access-bootstrap-and-sops-preparation) supersedes that access observation, not the need for a reviewed declarative transition.
- `stateVersion = "26.05"` from prior source; running stable revision `c257840`, not the repository's target revision.

#### Authorized access bootstrap and SOPS preparation

The operator confirmed working provider-console login and explicitly authorized a temporary access transition without a rebuild or reboot. `marcos` now exists with UID 1000, primary group `users` (100), `wheel`, a `0700` home and the reviewed public key in root-owned `0644` `/etc/ssh/authorized_keys.d/marcos`. A syntax-checked replacement of the live SSH configuration changes only `AllowUsers root` to `AllowUsers root marcos`; the SSH master was HUP-reloaded while a root control connection remained available. Fresh root and marcos key logins passed. Public-key-only authentication and disabled password/keyboard-interactive SSH remain intact. The operator privately set the password and confirmed interactive sudo; follow-up sees password status `P` and fresh-session sudo requiring authentication. The active generation remains `6j4ci94bwpn9hndhd9wqvqxdgkwkdc0m`.

This is a **temporary bootstrap**, not the final declarative deployment. The old configuration can overwrite these account/SSH changes on rebuild. The original SSH configuration is retained by `/root/infra-access-bootstrap.Cv2z4c/original-sshd-config`; root SSH remains available. Do not rebuild until password delivery and the other commissioning gates are resolved.

Under separate explicit authorization, the agent copied Racknerd's own locked stable `age` 1.3.2 package to its Nix store and generated a new dedicated identity at `/persist/var/lib/sops-nix/key.txt`, root-owned `0600` with a root-owned `0700` parent. Its public recipient was obtained through verified SSH and passed a public-canary encryption/decryption test. No private identity or account hash was returned. Its exact `.sops.yaml` rules permit only the existing operator plus this host's recipient for `racknerd.yaml` and `racknerd-tailscale.yaml`. The operator saved an encrypted recovery copy but **declined the recovery test**; `identityReviewed` remains false until dedicated-key early decryption and recovery are verified.

Tailscale still reports `NeedsLogin`; the live/backing state is the same bound file, root-owned `0600` under `0700` directories. No state contents were read or reset, no auth key was supplied, and no enrollment was attempted; Racknerd's rollout stays disabled until its own exact-tag key is provisioned near activation.

#### Commissioning review status (2026-09-10)

The operator attested the provider console/recovery, boot, and no-VPN network reviews over the read-only audit: KVM guest, provider-console login independent of SSH; ~2 GiB free `/boot` with Limine artifacts, BIOS-boot `vda1` (1 MiB BIOS-boot partition), `/boot`/Btrfs UUIDs matching the candidate and zero Btrfs error counters; uplink MAC `00:16:3c:ec:fa:6f` matching the candidate networkd profile with DNS, default route and direct public-IP SSH while Tailscale is logged out. Running-sshd host key equals the persisted backing key. For migration, the operator chose the ThinkPad stance: the untouched old Btrfs root subvolume is the recovery fallback instead of a fresh backup; persistent state, SSH host keys and the SOPS password delivery were reconciled in the candidate. The dedicated identity's early decryption/recovery and the staged `trusted-user` closure transport for `marcos` remain unverified, so `ready = false` and Racknerd is absent from `nixosConfigurations`/`deploy.nodes`.

The candidate still disables Tailscale enrollment and makes no storage changes. **First activation remains separately unauthorized** until the identity and closure-trust gates are completed; the retained root path is the intended staged bootstrap after those reviews. First-boot acceptance remains: marcos key login with root SSH refused, password sudo, persisted identities intact, retained Tailscale **backing state** with its reviewed root-only permissions and the intended service/enrollment **disabled**, bounded journals/fail2ban fresh, zero failed units, and Limine fallback boot from the console. Do not require a running `NeedsLogin` daemon in a candidate that disables Tailscale; daemon/enrollment/tag/traffic acceptance belongs to a separately authorized rollout.

### bastion — `192.168.2.2`

- UGREEN DXP4800 Plus, Intel Pentium Gold 8505, ~62 GiB RAM; UEFI Limine. Target hardware scan adapted; retain existing NVRAM policy pending boot review.
- OS NVMe: 128 GB, by-id `nvme-eui.6479a7a2ea200e8e`. ESP UUID `418B-E89E`, 1 GiB (~126 MiB used). Btrfs UUID `e56256b8-c591-497b-ad32-d4040bbb83be`, subvolumes `root`, `nix`, `persist`. `/persist` **stays on NVMe**.
- Data pool `tank`, GUID `7246454901288299061`, existing two-disk mirror:
  - `ata-ST4000VN006-3CW104_ZW63V4FM-part1`
  - `ata-TOSHIBA_HDWG440_2270A00MFZ1G-part1`
- Pool ONLINE, ~1.66 TiB allocated, zero reported data errors; last scrub 2026-08-31 repaired 0 B. Compatibility property `openzfs-2.4` observed, not set/upgraded by this repository. Health and a mirror do **not** establish backups or a verified restore.
- Retain hostId `ebbb349e` and the eight observed legacy mounts: `/srv`, `/srv/{containers,frigate,immich,nextcloud,nixflix,yuvomi,offsite-stage}`. All use matching `tank/srv/...` datasets except `/srv/offsite-stage` → `tank/offsite-stage`. No ZFS root, new datasets, properties, encryption, shares or migration. Preserve the prior monthly scrub schedule for `tank` only.
- The previous source has a custom strict GUID/topology/health importer. The candidate uses upstream named import with **both force-import flags false**; upstream may accept a degraded pool after waiting. These policies are not equivalent. Review identity, degraded-pool handling, mount failure behavior and restore before setting `fleet.nas.storageReviewed`. A future service must depend on its exact data mount, never fall back to an ordinary `/srv` directory.
- Networkd DHCPv4 on MAC `6c:1f:f7:56:0a:10`; observed lease `192.168.2.2/24` and gateway/DNS `192.168.2.1`, IPv6 RA/link-local disabled on this uplink. Second NIC remains unconfigured.
- Root-only key SSH; same staged non-root transition as racknerd. `stateVersion = "26.05"` from prior source; running stable revision `c257840`.

## Access and state migration checklist — no execution authorized

1. Preserve independent console/rescue access, old boot generations, backups and an actually tested restore. `bootReviewed`, `migrationReviewed`, network/user/provider/NAS flags and `ready` are real review records, not check bypasses.
2. Before removing root SSH on servers, separately provision and test `marcos` plus the chosen public key and password sudo while old access is still available. Do not relax the target's root-login restriction or disable deploy-rs rollback. A separate, authorized compatibility step on the old configuration is needed; this candidate is not that step.
3. Reused operator key fingerprint: `SHA256:mZ36DV6PDIt0lmhfqrO9qKKxQSlmQ7iMiH1NNYWvDRI`. Verified existing server host fingerprints at connection:
   - racknerd: `SHA256:z/SmBv5vFpmgQ6GKvkCVuTm/MoWQdJz0LOOEt2IMECQ`
   - bastion: `SHA256:5io89FRLljarJrjAHPbPo3AV5F7ssL6hJtYo42JxOVQ`
4. Password delivery uses [SOPS](../secrets/README.md), not manual `/persist/secrets` files. Use [current credential/boot evidence](#current-status) before deciding what remains to verify; ThinkPad's completed password/Wi-Fi boot acceptance does not satisfy another host's or a later secret's requirements. Missing bindings or an unlisted shared-password recipient lock only the **unactivated candidate**; an unreviewed identity independently blocks commissioning. Installed credentials are untouched. Preserve intended hashes in secure SOPS editing, never plaintext Git/Nix/store/logs. Do not reuse ThinkPad ciphertext on other hosts, widen recipients or generate/rotate credentials without approval.
5. Existing SSH host keys live in `/persist/etc/ssh`. The baseline's impermanence declarations expose those same backing key files at `/etc/ssh`; verify fingerprints and modes without rotating keys. Audit machine ID, random seed, NixOS allocation state, timers/time sync, NetworkManager state, Bolt state, power profile state, server journal and fail2ban database. Verify `/persist` parent ownership/modes; impermanence can propagate backing-parent modes onto ephemeral `/etc` and `/var`. Do not persist `/etc` or `/var` wholesale.
6. ThinkPad already mounts `/home` separately; never copy it to `/persist/home` or also bind it through impermanence. Retain ownership/UIDs. Old user-managed dotfiles/Home Manager links may reference removed desktop packages; audit these before login acceptance. ThinkPad's new desktop retains collision checks and isolates Noctalia config/state/data from old preferences; follow its [activation checklist](desktop.md#activation-and-acceptance-checklist). Existing application profiles/data are not deleted.
7. ThinkPad's accepted boots removed its old NetworkManager helper/file-secret agent and verified root-owned profile delivery; campus authentication/certificate rejection remain open as recorded in [current status](#current-status). Reconcile duplicate profiles and verify service ordering and CA/name validation for the transition. Preserving connection-profile files alone does **not** prove Wi-Fi credentials will work without the old agent/secret delivery. Securely migrate/verify the necessary runtime connection credentials without exposing them; test networking before acknowledging `networkReviewed`. Remove Tailscale only after verifying required routing and SSH reachability without it, with console access retained. Current successful SSH is not proof of post-removal connectivity. Preserve old `/persist/var/lib/tailscale` backing state without logging its contents; the [staged Tailscale design](tailscale.md) now requires its identity/migration review before enabling enrollment. Likewise unused prior service data remains intact.
8. Choose closure transport separately: signed closures require provisioned target public trust and an operator key outside Git; `trusted-user` would explicitly grant root-equivalent Nix access. Password sudo alone does not establish closure trust. Interactive deploy-rs sessions must be able to prompt.
9. After resolving genuine blockers: `devenv tasks run repo:fmt`, `devenv tasks run repo:check-full`, then approve readiness and run `devenv shell ready HOST` / `devenv shell build HOST`. Those commands do not authorize deployment. Review the built initrd, bootloader and persistence transition before any separately authorized activation/reboot. Perform two-boot persistence, SSH, network, boot-generation and ZFS acceptance with a console. Record new results against [current status](#current-status), without reclassifying earlier completed acceptance as pending or treating it as proof for a different candidate.
